# Copy to .env for local development. NEVER set these in a real Netlify
# deploy context (Project configuration > Environment variables) - their
# absence there is what keeps the auth bypass local-only.
DEV_AUTH_BYPASS=true
VITE_DEV_AUTH_BYPASS=true
